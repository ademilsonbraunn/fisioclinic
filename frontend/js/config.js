// URL base do backend — único lugar que precisa ser alterado para trocar de ambiente.
// Em produção, sobrescrever via variável de ambiente no build ou ajustar este valor.
export const API_BASE_URL = 'http://localhost:8080/api';
